.header
  .div style="margin-bottom: -30px;"
    | Realtime SMS App
  .flex-row.apart
    .div
    -if user_signed_in?
      = button_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-secondary', style: "margin-top: -20px; margin-right: 10px;"
    - else
      .div style="padding: 18px;"
style
  |
    .header {
      color: white;
      position: fixed;
      top: 0;
      width: 100%;
      background-color: #658eab;
      text-align: center;
      font-size: 1.6rem;
      padding: 10px;
      box-shadow: 0 1px 5px rgba(0,0,0,0.1);
      padding-left: 0px;
    }
